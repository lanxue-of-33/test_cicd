# 2026-09-12

## 创建 PMD_React 前端项目
- 在 `E:/Git_Project/test_cicd/PMD_React` 下新建 React + TypeScript + Vite 项目（React 18.3 + Vite 5.4 + TS 5.5）。
- 提供简单测试页面 `src/App.tsx`：计数器、输入框回显、深/浅色主题切换。
- 踩坑：tsconfig.node.json 同时 `composite:true` 与 `noEmit:true` 触发 TS6310，已将 emit 产物指向 `node_modules/.tmp` 解决。
- `npm run build` 通过；`npm run dev` 运行于 http://localhost:5173/。
